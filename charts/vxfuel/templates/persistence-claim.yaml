apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.global.appName }}-persist-claim
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 19Gi

